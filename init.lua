-- =============================================================================
-- 文件位置: init.lua
-- 说明: Neovim 入口配置文件，负责按顺序加载核心选项、按键映射与插件管理器
-- =============================================================================

-- 1. 加载核心基础选项与快捷键 (必须最先加载)
require("core.options")
require("core.keymaps")

-- 2. 启动 Lazy.nvim 插件管理器 (自动下载与加载插件)
require("core.lazy")

-- 3. 启动隔天随机主题切换逻辑与后台检测定时器
require("plugins.colorscheme").setup_auto_switch()
